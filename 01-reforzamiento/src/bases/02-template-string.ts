const firstName = 'Maria';
const lastName = "O'Neal"; /* Dobles comillas para cuando lo que escribes tiene apostrofe */
const fullName = `${firstName} ${lastName}`; /* Para concatenar texto */
console.log(fullName);